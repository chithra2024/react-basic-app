Instructions to Use:

Copy all these files into a new directory.
Run npm install.
Run npm run dev.
Open the local server address in your browser.
This setup uses:

Vite as the build tool
React 19 (per upgrade guide)
Tailwind CSS (configured per Vite guide)
Hubot Sans from Google Fonts, with a custom Tailwind fontFamily and default CSS



PS C:\Users\saravanan.1.p\Downloads\files> npm install

added 129 packages, and audited 130 packages in 24s

35 packages are looking for funding
  run `npm fund` for details

2 moderate severity vulnerabilities

To address all issues (including breaking changes), run:
  npm audit fix --force

Run `npm audit` for details.

npm install @vitejs/plugin-react --save-dev